server:
  servlet:
    context-path: /springboot4
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/zhou_db?serverTimezone=GMT
    username: root
    password: 1234
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true

